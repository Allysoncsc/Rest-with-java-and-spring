spring:
  application:
    name: rest-with-java-spring-erudio
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/rest_java?useTimezone=true&serverTimezone=UTC
    username: root
    password: ec@vxx123
  jpa:
    hibernate:
      ddl-auto: none nb /
  properties:
    hibernate:
      dialect: org.hibernate.dialect.MySQLDialect
  show-sql: false
  open-in-view: false
logging:
  level:
    root: WARN
#    br.com.allysoncsc: DEBUG o ideal é rodar em warn ou error

